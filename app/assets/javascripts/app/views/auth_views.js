App.LoginView = Ember.View.extend({
  classNames: ['l-guest-main'],
  templateName: 'login'
});

App.SignupView = Ember.View.extend({
  classNames: ['l-guest-main'],
  templateName: 'login'
});

App.PasswordResetView = Ember.View.extend({
  classNames: ['l-guest-main'],
  templateName: 'login'
});
