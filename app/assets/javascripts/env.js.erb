window.ENV = {
  FEATURES: {
    'string-humanize': true,
  }
};

window.ENV.OAUTH_ID     = "<%= ENV['OAUTH_ID'] %>";
window.ENV.OAUTH_SECRET = "<%= ENV['OAUTH_SECRET'] %>";
window.ENV.API_ENDPOINT = "<%= ENV['API_ENDPOINT'] %>";
